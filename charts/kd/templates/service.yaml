apiVersion: v1
kind: Service
metadata:
  name: {{ include "kd.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kd.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "kd.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
